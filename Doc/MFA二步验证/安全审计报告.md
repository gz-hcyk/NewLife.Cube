# NewLife.Cube MFA / 认证链路 全面安全审计报告

| 项 | 内容 |
|----|------|
| 版本 | **v2.0**（相对 v1.0 复审 + SSO 覆盖后全量深度审计） |
| 日期 | 2026-09-23 |
| 分支 | `cursor/mfa-two-factor-7061`（相对 `dev`） |
| 审计角色 | 网络信息安全 / 进攻性安全视角（白盒为主，灰盒逻辑渗透推演） |
| 合规参照 | OWASP ASVS 4.0（V2/V3/V4）、OWASP Top 10 2021、等保 2.0 身份鉴别增强、GB/T 35273（个人信息安全规范）、个人信息保护法（最小必要/告知/存储） |

---

## 1. 执行摘要

| 维度 | 结论 |
|------|------|
| 总体风险 | **高**：P0（挑战令牌换绑、无限速、默认密钥、外发 QR）已在本分支修复；SSO 已纳入 MFA；**仍存在可绕过 MFA 策略的登录入口与多项高危面** |
| 最严重残留 | **短信 / 邮箱验证码登录完全绕过 MFA**（Critical） |
| 个人信息 | 挑战页展示 DisplayName；`mfaToken` 进 URL 易入日志/Referer；手机/邮箱状态页已脱敏 |
| 数据保护 | TOTP 密文依赖 `JwtSecret` 派生；恢复码 SHA256 无盐，库泄露可离线穷举 |
| 合并建议 | 修复 Critical（SMS/Mail 门闸）+ High（通道 CSRF/降级、Setup XSS、Logout 开放重定向、令牌出 URL）前不宜视为「MFA 已达标」 |

### 1.1 发现统计（v2.0）

| 严重度 | 数量 | 要点 |
|--------|------|------|
| Critical | **1** | 短信/邮箱登录旁路 MFA |
| High | **6** | 通道无确认+CSRF、mfaToken 查询串、Setup 页 returnUrl XSS、Logout 开放重定向、存量会话无再校验、恢复码弱哈希 |
| Medium | **6** | SSO 提前写登录态、forceBind 链式、JwtSecret 耦合、验证码轰炸面、密码先写 Cookie、会话固定 |
| Low / Info | 若干 | Html.Raw、Cube/NC API 不对称、解绑死按钮、静默恢复码等 |
| 已关闭（v1→v2） | 5 | 挑战令牌换绑、无限速、默认密钥、无确认重绑、外发 QR |

---

## 2. 审计范围、资产与方法

### 2.1 资产与攻击面

| 资产 | 说明 |
|------|------|
| 身份 | 密码 / 短信 / 邮件 / SSO(OAuth) / MFA 挑战与强制绑定 |
| 会话 | Cookie、`ManageProvider` Current、JWT（front-end token） |
| 机密 | TOTP Secret（AES）、恢复码哈希、`JwtSecret`、短信/邮件 OTP |
| 个人信息 | 手机、邮箱、DisplayName、登录 IP、审计日志 |
| 配置 | `EnableMfa` / `MfaRequired` / `MfaTokenExpire` / `MaxLoginError` |

### 2.2 方法矩阵

| 手段 | 做法 | 结果形态 |
|------|------|----------|
| **静态分析（白盒）** | 通读 `MfaService`/`Totp`/`UserService`/`SsoProvider`/`UserController`/`Mfa*.cshtml`、Setting、实体 | 数据流与控制流缺陷清单 |
| **差分审查** | `dev` → 本分支 MFA/SSO 变更 | 回归安全债与修复有效性 |
| **威胁建模** | STRIDE：伪造、篡改、否认、信息泄露、拒绝服务、提权 | 攻击树与优先级 |
| **灰盒逻辑渗透** | 推演「已知密码 / 持有短信 / 持 mfaToken / CSRF / XSS」路径 | 概念级复现步骤（无生产 exploit 载荷） |
| **注入 / XSS 专项** | 审查 Razor/`Html.Raw`/JS 字符串拼接、查询参数进脚本 | Setup 页 XSS、恢复码 Raw |
| **跨站 / CSRF** | 检查 AntiForgery、SameSite、敏感 POST | 通道启停/Disable 面 |
| **黑盒用例设计** | 见 §6 测试矩阵（可纳入 CI / 手工渗透） | 验收用例 |
| **个人信息 / 数据保护** | 脱敏、最小化、密钥分离、日志留存 | PIPL/35273 对照 |

**不在本期**：全量依赖 CVE SCA、生产环境破坏性爆破、主题皮肤全站 XSS 爬取、物理与社工。

### 2.3 关键代码路径

- `NewLife.CubeNC/Services/MfaService.cs`、`UserService.cs`
- `NewLife.CubeNC/Security/Totp.cs`
- `NewLife.CubeNC/Areas/Admin/Controllers/UserController.cs`（MFA Actions）
- `NewLife.CubeNC/Areas/Admin/Views/User/Mfa*.cshtml`
- `NewLife.Cube/Web/SsoProvider.cs`、两端 `SsoController.cs`
- `NewLife.Cube/Entity/用户MFA*.cs`、`Setting.cs`
- `XUnitTest/MfaServiceTests.cs`、`TotpTests.cs`

---

## 3. 详细发现

### C-1 【Critical】短信 / 邮箱验证码登录绕过 MFA

| 项 | 内容 |
|----|------|
| 分类 | 认证绕过 / 策略失效（系统安全） |
| 位置 | `UserService.Login` → `LoginBySms` / `LoginByMail`（约 115–125、325、411 行）直接 `CompleteLogin`；仅 `LoginByPassword` 调用 `EvaluateAfterPassword`（约 180 行） |
| 攻击场景 | 用户已启用 TOTP MFA。攻击者若能收短信/邮件登录码（SIM 交换、邮箱沦陷、社工），或内部人员走「验证码登录」，**无需第二因子**即可获 Cookie/JWT 进后台 |
| 影响 | 「强制 MFA」产品承诺被击穿；与密码+SSO 门闸不一致，形成最短路径 |
| 复现草图 | ① `EnableMfa=true` 且用户已绑 TOTP ② `LoginCategory=Phone/Email` 提交正确 OTP ③ 响应直接 Token，无 `code=1002` |
| 修复 | `LoginBySms`/`LoginByMail` 在建会话前复用 `EvaluateAfterLogin`；需挑战则 Logout + `CreateChallenge`；产品明确「验证码登录 ≠ 已完成第二因子」 |
| 合规 | ASVS V2.8 不满足；等保身份鉴别增强「多因子」口径不一致 |

---

### H-1 【High】`MfaChannel` 无第二因子确认 + 缺 CSRF 防护

| 项 | 内容 |
|----|------|
| 分类 | 提权降级 / CSRF（系统安全） |
| 位置 | `UserController.MfaChannel`；`MfaService.SetChannel`（约 357–392 行）；`Mfa.cshtml` 裸 `$.ajax` POST |
| 攻击场景 | 已登录会话被 XSS/CSRF：关闭短信/邮件通道或关至零因子使 `Enable=false`，静默拆除 MFA。对比 `Disable`/`RegenerateBackupCodes` 已有 `EnsureConfirm` |
| 修复 | 启停通道、关 MFA、换绑统一二次确认；`[ValidateAntiForgeryToken]` 或自定义头 + SameSite；禁止无确认降至零因子 |
| 个人信息 | 攻击者可持续访问账号下业务与个人数据 |

---

### H-2 【High】SSO 将 `mfaToken` 置于 URL 查询串

| 项 | 内容 |
|----|------|
| 分类 | 会话/令牌泄露（系统安全 + 个人信息） |
| 位置 | `SsoProvider.TryPrepareMfaRedirect`（约 240–247 行）；`SsoController` 再附 `r=` |
| 攻击场景 | 浏览器历史、反向代理/WAF 访问日志、Referer（外链资源）泄露令牌；有效期内可完成挑战或强制绑定并登录 |
| 说明 | `Rand.NextString(32)` **抗猜测**，**不抗泄露** |
| 修复 | HttpOnly Cookie / TempData / 一次性短 ticket；缩短 TTL；禁止日志打印完整 token |

---

### H-3 【High】`MfaSetup.cshtml` 将 `returnUrl` 直接嵌入 JavaScript（XSS）

| 项 | 内容 |
|----|------|
| 分类 | 跨站脚本（注入 / XSS） |
| 位置 | `MfaSetup.cshtml` 约第 59 行：`r: '@returnUrl'`；SSO/登录可把可控 `r` 带到页面 |
| 攻击场景 | 构造含引号或 `</script>` 的 `r`，在强制绑定页执行脚本 → 窃取页内 `mfaToken`、代提绑定、钓鱼 |
| 说明 | `VerifyMfa`/`MfaTotpConfirm` 服务端 `Url.IsLocalUrl` **挡开放重定向，不挡视图 XSS** |
| 修复 | `JavaScriptStringEncode` / `Json.Serialize`；服务端会话保存 returnUrl，视图不回显原始串 |

---

### H-4 【High】`Logout` 开放重定向

| 项 | 内容 |
|----|------|
| 分类 | 开放重定向 / 钓鱼 |
| 位置 | `UserController.Logout`（约 765–791 行）`Redirect(returnUrl)` **无** `IsLocalUrl` |
| 攻击场景 | `GET .../Logout?r=https://evil.example` 注销后跳恶意站，配合「刚登出」社工钓鱼窃密 |
| 修复 | 与 Login/VerifyMfa 一致：仅本地 URL 或白名单 |

---

### H-5 【High】启用 MFA 后存量会话无再校验

| 项 | 内容 |
|----|------|
| 分类 | 策略时效 / 访问控制 |
| 位置 | 门闸仅在密码登录与 SSO `OnLogin`；中间件无「MFA 已满足」声明 |
| 攻击场景 | 开启 `EnableMfa`/`MfaRequired` 前已登录的 Cookie/JWT 持续有效直至过期 |
| 修复 | 开启全局 MFA 时批量作废会话或强制声明位；认证管道检查 MFA claim |

---

### H-6 【High】恢复码 SHA256 无盐，熵约 40 bit

| 项 | 内容 |
|----|------|
| 分类 | 数据保护 / 离线破解 |
| 位置 | `GenerateBackupCodes`（8 位字母数字）+ `HashCode` SHA256 无 per-code 盐（约 476–508 行） |
| 攻击场景 | `UserMfa.BackupCodes` 库表泄露后 GPU 穷举，恢复码可直接过 MFA |
| 修复 | Argon2id/bcrypt + 随机盐；提高码熵（≥128 bit 等效）；限制失败与剩余次数 |
| 合规 | GB/T 35273 存储保护；ASVS V2.9 部分不满足 |

---

### M-1 【Medium】SSO 在 MFA 完成前已更新 LastLogin / Online

| 项 | 内容 |
|----|------|
| 位置 | `SsoProvider.OnLogin` 约 181–190 行先落库，再 `TryPrepareMfaRedirect` |
| 影响 | 审计与在线状态「已登录」失真；**Cookie 层面** MFA 前会 Logout，非直接会话绕过 |
| 修复 | 登录计数/在线状态挪到 MFA 成功（对齐 `CompleteLogin`） |

---

### M-2 【Medium】`forceBind`（OAuth Bind）跳过 MFA

| 项 | 内容 |
|----|------|
| 位置 | `OnLogin` `if (!forceBind && ...)`（约 197–202 行） |
| 影响 | 若会话来自 C-1 短信登录，可再绑第三方形成持久无 MFA 入口（链式放大） |
| 修复 | Bind 要求已 MFA 会话；文档明确约束 |

---

### M-3 【Medium】`JwtSecret` 弱默认且与 TOTP 保护密钥耦合

| 项 | 内容 |
|----|------|
| 位置 | `Setting.OnLoaded` 可能生成 `HS256:{Rand.NextString(16)}`；`DeriveKey` SHA256(`JwtSecret`) |
| 影响 | 配置泄露可解 TOTP；轮换 JWT 导致全部 TOTP 密文失效 |
| 修复 | 独立 `MfaProtectionKey`（≥256 bit）；禁止弱默认上线 |

---

### M-4 【Medium】SMS/Mail 挑战码发送面（轰炸 / 枚举）

| 项 | 内容 |
|----|------|
| 位置 | `GuardSendRate`：同号 60s、同 IP 10 分钟 5 次；`MaxLoginError<=0` 时校验无限速 |
| 影响 | 费用/骚扰；弱配置下 6 位码在线猜解风险上升 |
| 修复 | 用户日限额；强制 `MaxLoginError>0`；统一错误文案 |

---

### M-5 【Medium】密码登录先 `Login`（写 Cookie）再 `Logout`

| 项 | 内容 |
|----|------|
| 位置 | `LoginByPassword` 约 168–196 行 |
| 影响 | 同请求内通常可接受；异常中间件/代理只认首个 Set-Cookie 时存在边角窗口 |
| 修复 | 密码校验用只读 Check，通过后再发挑战，避免 MFA 前写会话 |

---

### M-6 【Medium】MFA 成功后未再生会话 ID（会话固定）

| 项 | 内容 |
|----|------|
| 位置 | `CompleteLogin` 无 Session 再生 |
| 修复 | 登录/MFA 成功时 `Session.Regenerate` / 换发 Cookie |

---

### L / Info

| ID | 说明 | 建议 |
|----|------|------|
| L-1 | `Mfa.cshtml` `@Html.Raw(backups)` + `.html(codes)` | JSON 编码 + `text()` |
| L-2 | Cube WebApi 缺绑定/关闭等管理 API（登录门闸共用，非旁路） | API 补齐或文档约束部署 |
| L-3 | 解绑按钮无 Action；`SetChannel` 静默生成恢复码不展示 | 补齐或移除 |
| L-4 | TOTP/`==` 非常量时间；备份码比较已哈希 | 固定时间比较 |
| L-5 | 挑战页 DisplayName 暴露 | 可接受；避免多余 PII |
| I-1 | SSO MFA 前不发 front-end token / 不记在线（Controller 层） | 保持 |
| I-2 | 挑战/setup 令牌用途隔离、失败限速（MaxLoginError>0） | 已加固，保持回归测试 |

---

## 4. v1.0 发现复审（修复有效性）

| v1 ID | 状态 | 验证说明 |
|-------|------|----------|
| C-1 挑战令牌换绑 TOTP | **已关闭** | `ResolveTokenUser(requireSetup:true)`；挑战不可绑；测试 ST-01 |
| H-1 MFA 无限速 | **已关闭** | token/user/IP 计数，达阈作废；测试 ST-03（依赖 MaxLoginError>0） |
| H-2 默认加密密钥 / 明文回退 | **已关闭** | 无 JwtSecret 拒绝绑定；解密失败强制重绑 |
| H-3 SMS/Mail/SSO 绕过 | **部分关闭** | **SSO 已覆盖**；SMS/Mail 仍为 Critical（本报告 C-1） |
| H-4 已登录无确认重绑 | **已关闭** | `StartTotpSetup` 强制旧因子/密码 |
| M-1 外部 QR | **已关闭** | 本地文本密钥，无 qrserver |
| M-2 CSRF 等 | **未关闭** | 见 H-1 |
| M-4 恢复码并发双花 | **未关闭** | 仍读改写无锁，并入运维风险 |

---

## 5. 合规与个人信息保护对照

### 5.1 OWASP ASVS / Top 10

| 条目 / 类别 | 现状 |
|-------------|------|
| V2.2 防爆破 | MFA 校验有限速（配置依赖）；SMS/Mail 登录另计 → 部分满足 |
| V2.8 多因子覆盖 | 密码+SSO 满足；短信/邮件登录 **不满足** |
| V2.9 恢复 | 不明文落库好；无盐弱熵 → 部分满足 |
| V2.10 密钥 | 无默认密钥好；与 JWT 耦合 → 部分满足 |
| V3 会话 | MFA 前 Logout 好；会话固定/先写 Cookie → 部分满足 |
| V4 / A01 访问控制 | 通道降级、存量会话 → 部分满足 |
| A03 注入 | Setup returnUrl XSS → **不满足**（局部） |
| A07 认证失败 | Logout 开放重定向、令牌 URL → 部分满足 |

### 5.2 个人信息保护法 / GB/T 35273（摘要）

| 原则 | 现状 | 建议 |
|------|------|------|
| 最小必要 | 挑战页 DisplayName；Methods 列表合理 | 避免日志完整 token/手机号 |
| 存储保护 | TOTP AES；恢复码哈希 | 恢复码加盐 KDF；密钥独立 |
| 跨境/第三方 | 外发 QR 已移除 | 保持密钥不出域 |
| 安全事件 | 审计日志有 MFA 成功/失败 | 脱敏 token；告警失败峰值 |
| 告知同意 | MFA 绑定属安全增强 | 产品侧说明恢复码保管责任 |

---

## 6. 渗透 / 黑白盒测试矩阵（建议 CI + 手工）

| 编号 | 类型 | 用例 | 期望 |
|------|------|------|------|
| ST-01 | 白盒/回归 | 挑战 `mfaToken` 调 `MfaTotpStart` | **拒绝**（已有） |
| ST-02 | 白盒 | 已绑定无旧因子 `MfaTotpConfirm` | **拒绝**（已有） |
| ST-03 | 白盒 | `VerifyMfa` 连续错误 ≥ MaxLoginError | 作废 token（已有） |
| ST-04 | **黑盒** | `EnableMfa` 下短信/邮件登录 | **须 MFA 或拒绝**（当前失败＝漏洞） |
| ST-05 | 并发 | 同一恢复码双请求 | 仅一次成功 |
| ST-06 | 配置 | 无 JwtSecret 绑 TOTP | 失败 |
| ST-07 | 流量 | 绑定页无外发含 secret 的 URL | 通过 |
| ST-08 | XSS | `MfaSetup?r=` 含 `'` / `</script>` | 不执行脚本 |
| ST-09 | 开放重定向 | `Logout?r=https://evil` | 拒绝或本地默认 |
| ST-10 | CSRF | 跨站 POST `MfaChannel` Enable=false | 拒绝 |
| ST-11 | SSO | OAuth 成功后未 MFA 前访问需登录 API | 401/登录页 |
| ST-12 | 令牌 | 代理日志检索 `mfaToken=` | 不应出现（改 Cookie 后） |
| ST-13 | 注入 | MFA Code/Method 超长与特殊字符 | 安全失败无堆栈泄露 |
| ST-14 | 枚举 | 错误提示是否区分「未启用通道」 | 统一文案更佳 |

当前自动化：`dotnet test --filter "FullyQualifiedName~Mfa\|FullyQualifiedName~Totp"` → **12 通过**；**未**覆盖 ST-04/08/09/10。

---

## 7. 已验证相对稳健的设计

1. 密码登录需挑战/绑定时 `Logout` 后再发令牌，避免「未过 MFA 持完整会话」。  
2. SSO：`TryPrepareMfaRedirect` + Controller 提前 Redirect，不记在线、不发 front-end token。  
3. 挑战令牌与 setup 令牌用途隔离；校验成功消费令牌。  
4. TOTP：CSPRNG 160-bit、RFC 6238；落库 AES；状态页手机/邮箱脱敏。  
5. 恢复码不明文落库；Disable/重建恢复码需二次确认。  
6. Cube 与 CubeNC 登录/`MfaService` 链接编译，两端 SSO 门闸对齐。  
7. MFA 成功路径 `Url.IsLocalUrl`（登录后跳转）。  

---

## 8. 修复优先级与验收

### P0（发布前必须）

1. **C-1** 短信/邮箱登录接入 `EvaluateAfterLogin`  
2. **H-3** Setup 页 `returnUrl` JS 编码  
3. **H-4** Logout `IsLocalUrl`  

### P1（短期）

4. **H-1** 通道启停二次确认 + Anti-Forgery  
5. **H-2** `mfaToken` 移出 URL  
6. **H-5** 开启 MFA 时失效存量会话  
7. **H-6** 恢复码加盐 KDF + 提高熵  

### P2

8. M-1～M-6（审计时序、forceBind、独立保护密钥、轰炸限额、免 Cookie 校验、会话再生）  
9. L 系列清理  

### 验收标准

- ST-04、ST-08、ST-09、ST-01～03、ST-07 自动化或手工签字通过  
- 架构文档与产品承诺：写明「覆盖密码 + SSO；短信/邮件登录是否计入 MFA」与实现一致  
- 无未缓解 Critical；High 有计划与截止日期  

---

## 9. 结论

本轮在 **系统安全、个人信息保护、数据保护** 三维下，对 MFA 与关联认证链路做了静态白盒、差分复审、STRIDE 威胁建模、注入/XSS/CSRF/开放重定向专项及黑盒用例设计。

**已加固成果**（v1 P0 + SSO 门闸）有效：挑战/绑定令牌隔离、校验限速、禁止默认密钥、本地密钥展示、SSO 与密码共用 `EvaluateAfterLogin`。

**当前最大风险**仍是 **短信/邮箱一因子登录旁路 MFA**；叠加通道 CSRF 降级、Setup XSS、Logout 开放重定向与令牌 URL 泄露，在「已开启 MFA」宣传口径下仍可能被进攻方以最短路径拿下会话。

**建议**：按 §8 P0→P1 修复并跑通 §6 矩阵后，再将 MFA 标为生产就绪；修复前对外承诺须显式排除「验证码登录」或标注已知限制。

---

*报告说明：基于源码静态分析与逻辑渗透推演；未在生产环境实施破坏性攻击或撞库。概念级复现步骤仅供修复与验证，禁止用于未授权测试。*

### 附录 A：攻击路径示意（简化）

```
[密码] ──MFA门闸──► 挑战/绑定 ──► 会话     ✅
[SSO]  ──MFA门闸──► 挑战/绑定 ──► 会话     ✅（本分支）
[短信/邮件登录] ──────────────► 会话     ❌ Critical
[已登录 + CSRF 关通道] ────────► 无 MFA    ❌ High
[SSO + mfaToken 泄露] ─────────► 窗口接管  ❌ High
[Setup?r=XSS] ─────────────────► 令牌窃取  ❌ High
```

### 附录 B：文档与代码索引

| 文档/代码 | 路径 |
|-----------|------|
| 本报告 | `Doc/MFA二步验证/安全审计报告.md` |
| 架构 | `Doc/MFA二步验证/架构设计.md` |
| 需求 / 功能清单 / 原型 | `Doc/MFA二步验证/` |
| 核心服务 | `NewLife.CubeNC/Services/MfaService.cs` |
| 登录门闸 | `NewLife.CubeNC/Services/UserService.cs` |
| SSO | `NewLife.Cube/Web/SsoProvider.cs` |
